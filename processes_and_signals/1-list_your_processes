#!/usr/bin/env bash
# This script displays a list of currently running processes for all users,
# including those without a TTY, in a user-oriented format with process hierarchy.

# Use the ps command to display all processes in a user-oriented format
ps -e -o user,pid,ppid,cmd --forest
